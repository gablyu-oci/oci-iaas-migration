# Adapted from OCI provider docs — route table attachment to subnet
resource "oci_core_route_table_attachment" "{{ label }}" {
  subnet_id      = {{ subnet_id }}
  route_table_id = {{ route_table_id }}
}
