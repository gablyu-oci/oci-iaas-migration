# Security list attachment — modeled as a reference update on the subnet
# In practice, security_list_ids is set on the oci_core_subnet resource.
# This template exists as a placeholder for the structured output pipeline.
# aws_source_id = n/a
# Attach security list {{ security_list_id }} to subnet {{ subnet_id }}
# NOTE: This is handled within the subnet resource's security_list_ids attribute.
# No separate Terraform resource is needed.
